spring:
  main:
    banner-mode: console
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:sqlserver://DBDEVAW01.dev.activenetwork.com:1433;databaseName=cart_service
    username: fnd_dev_usr
    password: fnd_dev_usr$2008
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    hikari:
      poolName: oltpPool
      connectionTimeout: 30000
      transactionIsolation: TRANSACTION_READ_COMMITTED
      autoCommit: true
      maxLifetime: 1800000
      idleTimeout: 600000
      minIdle: 5
      maximumPoolSize: 100
      leakDetectionThreshold: 0

mybatis:
  type-aliases-package: com.active.services.cart.mybatis.types

server:
  port: 8273

management:
  endpoints:
    web:
      base-path: /management
      exposure:
        include: '*'
  endpoint:
    shutdown:
      enabled: false